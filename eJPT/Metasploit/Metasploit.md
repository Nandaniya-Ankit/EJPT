1. Port_scanning 
	-> `/auxiliary/scanner/portscan/tcp` : MSF

2.  `info` : get info of any module after `use` 
3. `analyze` : analyze MSFDB stored scans and ouputs , open ports and service and tells which are the exploits ready to use.
4. 